-- @noindex
-- rearkitekt/defs/layout.lua
-- Spacing, padding, and rounding constants

local M = {}

-- ============================================================================
-- PADDING
-- ============================================================================
M.PADDING = {
    none = 0,
    xs = 2,
    sm = 4,
    md = 8,
    lg = 12,
    xl = 16,
    xxl = 24,
}

-- ============================================================================
-- SPACING (gaps between elements)
-- ============================================================================
M.SPACING = {
    none = 0,
    xs = 2,
    sm = 4,
    md = 8,
    lg = 12,
    xl = 16,
}

-- ============================================================================
-- ROUNDING (border radius)
-- ============================================================================
M.ROUNDING = {
    none = 0,
    sm = 2,
    md = 4,
    lg = 8,
    xl = 12,
    full = 9999,    -- Fully rounded (pill shape)
}

-- ============================================================================
-- BORDERS
-- ============================================================================
M.BORDER = {
    none = 0,
    thin = 1,
    normal = 2,
    thick = 3,
}

return M
